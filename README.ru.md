# Phase Rotation (RX-style) для REAPER

Аналог модуля **Phase из iZotope RX** для айтемов REAPER. [English README](README.md).

Голос почти всегда записывается несимметрично: положительные и отрицательные пики разной
высоты. Поворот фазы всех частот на один и тот же угол не меняет звучание, но
перераспределяет пики — форма волны выравнивается по центру, появляется запас по уровню.
Здесь есть всё то же, что в RX: **Suggest**, слайдеры **Left/Right rotation** со связкой,
**Adaptive phase rotation**, **Preview / Bypass** и **Render**.

| В RX                      | Здесь                                                                        |
|---------------------------|------------------------------------------------------------------------------|
| Suggest                   | Анализирует каждый выделенный айтем и ставит общий для каналов угол. Воспроизводит значения Suggest самого RX 10 (проверено по рендерам RX, см. `tests/RX_REFERENCE.md`); есть и критерий «минимальный пик» |
| Left / Right rotation [°] | Слайдеры −180…+180, поле ввода (клик — ввести число), кнопка Link             |
| Adaptive phase rotation   | JSFX пересчитывает угол каждые ~43 мс и плавно переходит между значениями     |
| Preview / Bypass          | Проигрывает айтем с начала; Bypass выключает take FX                          |
| Render                    | Необязательно: запекает поворот в новый тейк (оригинал остаётся). Поворот и так применяется вживую через take FX — Render нужен только чтобы форма волны на экране стала симметричной |
| Compare                   | Не реализовано — используйте Bypass и Undo                                    |

Всё неразрушающее: скрипт вставляет JSFX **«Phase Rotation (RX-style, Hilbert)»** как
take FX, и айтем сразу играет с повёрнутой фазой — без рендера и монтажа. **Render**
необязателен (RX вынужден рендерить, потому что правит файл; REAPER — нет): жмите его,
только если нужен новый тейк, у которого *отрисовка* волны покажет симметричную форму,
или чтобы убрать take FX. Знак угла — как в RX.

## Установка

### ReaPack (рекомендуется)

1. Extensions → ReaPack → Import repositories…
2. Вставьте `https://github.com/dennech/reaper-phase-rotation/raw/main/index.xml`
3. Установите **Phase Rotation (RX-style).lua** из репозитория *REAPER Phase Rotation*
   (JSFX ставится вместе со скриптом).

### Вручную

1. Скопируйте папку `Items` в `<ресурсы REAPER>/Scripts/` (можно переименовать, например
   `Scripts/Phase Rotation/`). Три файла должны лежать вместе:
   `Phase Rotation (RX-style).lua`, `phase_rotation_core.lua`, `phase_rotation.jsfx`.
2. Actions → Show action list → *New action…* → *Load ReaScript…* → выберите `.lua`.
3. При первом запуске скрипт сам скопирует `phase_rotation.jsfx` в
   `Effects/Phase Rotation/`, чтобы REAPER мог его загрузить.

Нужен REAPER 6.x или новее (проверено на 7.80, macOS). Расширения не требуются.

## Как пользоваться

1. Выделите один или несколько **аудио-айтемов** (моно или стерео).
2. Запустите действие **Script: Phase Rotation (RX-style).lua**.
3. Нажмите **Suggest** (или `S`). Для каждого айтема считается свой угол. В окне видны пик
   до/после, положительный/отрицательный пики и выигрыш по запасу, а также маленький
   полярный график «огибающая × фаза» (серый — до, синий — текущий поворот; ширина фигуры
   по горизонтали и есть пиковый уровень).
4. **Preview** (`Space`) / **Bypass** (`B`) — послушать. Слайдеры — ручная подстройка
   (Shift — точно, колесо — по 1°, двойной клик — 0°, клик по значению — ввести число).
5. **Render** запекает take FX в новый тейк. На время рендера трековые FX обходятся, так
   что применяется только поворот фазы; оригинальный тейк сохраняется (меняется в меню
   `...`). Undo работает как обычно.

Множественное выделение: **Suggest / Bypass / Render / Remove FX / Adaptive / Link
действуют на все выделенные айтемы**, слайдеры правят айтем, показанный в навигаторе
*Item i / n*.

**Adaptive phase rotation** следует за сигналом вместо одного фиксированного угла —
полезно, когда асимметрия меняется во времени (несколько дикторов, длинные дубли).
Время реакции задаётся в меню `...` (100 мс … 2 с). Как и в RX, режим рассчитан на речь;
на музыке обычно чище фиксированный угол.

JSFX можно использовать и отдельно (как track FX или take FX) — органы управления те же.

## Как это работает

* **Поворот**: `y = cos(θ)·x + sin(θ)·H{x}` (знак как в RX), где `H` — линейно-фазовый FIR-преобразователь
  Гильберта (окно Кайзера, 8193 отвода при ≤ 50 кГц, 16385 выше), применяемый FFT-свёрткой
  overlap-save. Ровный в пределах 0,01 дБ от 14 Гц. При 0° выход побитово равен входу;
  задержка (256 мс при 48 кГц) объявлена как PDC и полностью компенсируется REAPER — ничего
  не сдвигается во времени.
* **Suggest**: анализ на Lua считает тот же Гильберт (через встроенный FFT REAPER), строит
  гистограммы огибающей аналитического сигнала по фазе (шаг 0,125°) и ищет угол,
  минимизирующий `Σ|y|^8` — норму L8, «сглаженный пик». Именно этот критерий
  воспроизводит значения Suggest у iZotope RX 10 (+78° / +62° / +78° на трёх эталонных
  файлах, у нас +77,6° / +62,3° / +77,5°; подробности в `tests/RX_REFERENCE.md`).
  Альтернативный критерий (меню `...`) минимизирует точный сэмпловый пик `max |y|` и даёт
  на 0,3–0,6 дБ больше запаса, чем угол RX. 90 секунд стерео анализируются за ~0,4 с.
* **Adaptive**: JSFX оценивает угол минимального пика на подблоках по 43 мс (с
  упреждением, гейтом на тишине и гистерезисом между близкими минимумами) и сглаживает угол
  однополюсным фильтром (время реакции — в меню `...`).

## Ограничения

* Обрабатываются целые айтемы. Чтобы обработать участок — сначала разрежьте айтем.
* Стерео анализируется по каналам; при включённом **Link** берётся общий угол (как в RX).
  У айтемов с более чем двумя каналами обрабатываются каналы 1–2, остальные проходят без
  изменений.
* Анализ учитывает, как играет тейк (playrate, channel mode), но не другие take FX.
* Критерий — *сэмпловый* пик, как в RX. Одиночный щелчок может его перевесить — сначала
  уберите щелчки или задайте угол на слух.
* При живом воспроизведении JSFX добавляет 256 мс PDC-задержки; на рендер это не влияет.

## Тесты

`tests/reference.py` — независимая реализация на numpy (расчёт FIR, поиск угла,
синтетические асимметричные «голосовые» сигналы). `tests/run_tests.sh` запускает
изолированный экземпляр REAPER (отдельная папка ресурсов, ваш конфиг не трогается),
выполняет `tests/run_in_reaper.lua` (анализ, рендер с фиксированными углами, проверка PDC,
адаптивный рендер, обход трековых FX) и `tests/gui_smoke.lua` (сценарий кликов через
настоящий UI) и сверяет результаты с эталоном: углы совпадают до 0,125°, отрендеренный
звук — до ~1e-6. В `tests/RX_REFERENCE.md` — сравнение с iZotope RX 10 (углы, знак,
отрендеренное аудио).

## Авторы

Сделано [dennech](https://github.com/dennech) вместе с Claude. Поведение повторяет
задокументированную семантику модуля Phase из iZotope RX; код iZotope не используется.
Лицензия MIT.
